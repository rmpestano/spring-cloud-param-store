spring:
  application:
    name: 'demo'
  config:
    import: 'optional:aws-parameterstore:/'

demo:
  param: 'from app properties'

user.config.users:
  -  name: 'user1'
     pass: 'pass1'
     roles:
     - name: 'admin'
     - name: 'user'
